@model IEnumerable<CourseProject.list_of_specialty>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Specialty_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Spetialty_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Specialty_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Spetialty_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Specialty_id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Specialty_id }) |

        </td>
    </tr>
}

</table>
